---
import { BOOK } from '../lib/bookData';

// Il footer è organizzato in sezioni semantiche per aiutare sia gli utenti che
// i crawler a capire la struttura del sito. I link esterni portano `cta` per far
// scattare geo_cta_clicked via ctaTracking (listener delegato in Shell.astro).
type FooterLink = { label: string; href: string; cta?: string };
type FooterColumn = { id: string; heading: string; links: FooterLink[] };

const productCol: FooterColumn = {
  id: "product",
  heading: "Product",
  links: [
    { label: "Audit", href: "/", cta: "footer_audit" },
    { label: "Compare", href: "/compare/", cta: "footer_compare" },
    { label: "Analyze competitors", href: "/analyze-competitors/", cta: "footer_analyze_competitors" },
    { label: "AI citation checker", href: "/tools/ai-citation-checker/", cta: "footer_citation_checker" },
    { label: "Pricing", href: "/pricing/", cta: "footer_pricing" },
  ],
};

const resourcesCol: FooterColumn = {
  id: "resources",
  heading: "Resources",
  links: [
    { label: "Best GEO tools", href: "/best-geo-tools/", cta: "footer_best_geo_tools" },
    { label: "Guides", href: "/guides/", cta: "footer_guides" },
    { label: "State of GEO", href: "/state-of-geo/", cta: "footer_state_of_geo" },
    { label: "Methodology", href: "/methodology/", cta: "footer_methodology" },
    { label: "GitHub", href: "https://github.com/Auriti-Labs/geo-optimizer-skill", cta: "footer_github" },
    { label: "Docs", href: "https://auriti-labs.github.io/geo-optimizer-skill/", cta: "footer_docs" },
    { label: "PyPI", href: "https://pypi.org/project/geo-optimizer-skill/", cta: "footer_pypi" },
  ],
};

const companyCol: FooterColumn = {
  id: "company",
  heading: "Company",
  links: [
    { label: "About", href: "/about/", cta: "footer_about" },
    { label: "Transparency", href: "/transparent-geo-tool/", cta: "footer_transparency" },
    { label: "Instagram", href: "https://www.instagram.com/geoready.dev/", cta: "footer_instagram" },
    { label: "Privacy", href: "/privacy/", cta: "footer_privacy" },
    { label: "Cookies", href: "/cookie-policy/", cta: "footer_cookies" },
  ],
};

const columns: FooterColumn[] = [productCol, resourcesCol, companyCol];

// Restituisce gli attributi da applicare al link: apre in nuova scheda i link
// esterni e aggiunge il cta tracking sui contenuti (null se non serve).
const linkAttrs = (l: FooterLink) => {
  if (l.href.startsWith('http')) {
    return { target: '_blank', rel: 'noopener', 'data-cta': l.cta };
  }
  return { 'data-cta': l.cta };
};
---

<div class="mt-auto border-t border-rail bg-paper">
  <div class="mx-auto max-w-7xl px-6 py-12">
    <div class="grid gap-10 md:grid-cols-[1.2fr_2fr_1fr]">

      {/* Brand + tagline */}
      <div class="flex flex-col gap-4">
        <div class="flex items-center gap-2.5">
          <svg width="22" height="22" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <rect width="48" height="48" rx="4" fill="#0D9488"/>
            <path d="M11.74 13.71 A16 16 0 1 1 11.74 34.29" stroke="white" stroke-width="2.8" stroke-linecap="round"/>
            <path d="M8 24 C12 14 18 34 24 24 C30 14 36 34 40 24" stroke="white" stroke-width="2.1" stroke-linecap="round" stroke-opacity="0.72"/>
          </svg>
          <span class="font-head text-sm font-extrabold">
            <span class="text-pass-deep">Geo</span><span class="text-ink">Ready</span>
          </span>
          <span class="font-mono text-[10px] text-ink-mute">v{__ENGINE_VERSION__}</span>
        </div>
        <p class="max-w-[220px] text-xs leading-relaxed text-ink-mute">
          Open-source AI search visibility audit and monitoring. Built in Italy by Auriti Labs.
        </p>
      </div>

      {/* Link columns */}
      <div class="grid grid-cols-2 gap-8 sm:grid-cols-3">
        {columns.map((col) => (
          <nav aria-label={`Footer – ${col.heading}`}>
            <p class="mb-3 font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-ink-mute">{col.heading}</p>
            <ul class="space-y-2.5">
              {col.links.map((l) => (
                <li><a href={l.href} class="text-[13px] leading-relaxed text-ink-soft transition-colors hover:text-ink" {...linkAttrs(l)}>{l.label}</a></li>
              ))}
            </ul>
          </nav>
        ))}
      </div>

      {/* Book card — risalto a sé, asset premium a conversione */}
      <div class="flex flex-col justify-between rounded-[4px] border border-rail bg-white p-5">
        <div>
          <p class="font-mono text-[10px] uppercase tracking-[0.14em] text-pass-deep">The Book</p>
          <p class="mt-2 font-head text-[17px] font-extrabold leading-tight tracking-[-0.02em] text-ink">
            {BOOK.title}
          </p>
          <p class="mt-1.5 text-xs leading-relaxed text-ink-soft">
            Google non è più l'unica porta d'accesso ai clienti. Manuale tecnico per far citare
            il tuo sito da LLM e agenti AI, con protocolli validati su AutoGEO ICLR 2026 e KDD 2024.
            {BOOK.numberOfPages} pagine, paperback e Kindle.
          </p>
        </div>
        <a
          href="/book/"
          data-cta="footer_book"
          class="mt-4 inline-flex items-center justify-center gap-1.5 rounded-[4px] bg-pass-deep px-4 py-2 font-mono text-[11px] font-semibold uppercase tracking-[0.12em] text-white transition-colors hover:bg-pass"
        >
          <span>Get the book</span>
          <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <line x1="5" y1="12" x2="19" y2="12"/>
            <polyline points="12 5 19 12 12 19"/>
          </svg>
        </a>
      </div>
    </div>

    <div class="mt-10 flex flex-col items-center gap-5 border-t border-rail pt-6 text-center">
      <a href="https://launchpadly.co/startup/geoready" target="_blank" rel="noopener noreferrer" data-launchpadly-badge="geoready" data-launchpadly-badge-variant="light">
        <img src="https://launchpadly.co/embed/badges/startup/geoready.svg?variant=light" alt="Launchpadly Startup Directory" width="220" height="48" style="display:block;border:0;" />
      </a>
      <p class="text-xs text-ink-mute">
        Built in Italy by Auriti Labs. Designed for the global AI search ecosystem. Powered by Princeton KDD 2024 + AutoGEO ICLR 2026 research.
      </p>
    </div>
  </div>
</div>
